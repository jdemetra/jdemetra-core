<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.europa.ec.joinup.sat</groupId>
        <artifactId>demetra-parent</artifactId>
        <version>3.0.0-SNAPSHOT</version>
       <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>demetra-benchmarking-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    
    <name>JD3 - Benchmarking - Parent</name>
    
    <properties>
        <demetra.version>3.0.0-SNAPSHOT</demetra.version>
        <jd3-basic.version>1.0.0-SNAPSHOT</jd3-basic.version>
        <jd3-econometrics.version>1.0.0-SNAPSHOT</jd3-econometrics.version>
        <jd3-benchmarking.version>1.0.0-SNAPSHOT</jd3-benchmarking.version>
    </properties>
    
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-tstoolkit</artifactId>
                <version>${demetra.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-datatypes</artifactId>
                <version>${jd3-basic.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-data-tests</artifactId>
                <version>${jd3-basic.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-core</artifactId>
                <version>${jd3-basic.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-algorithms</artifactId>
                <version>${jd3-basic.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-ts</artifactId>
                <version>${jd3-basic.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-eco-datatypes</artifactId>
                <version>${jd3-econometrics.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-eco</artifactId>
                <version>${jd3-econometrics.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-ssf</artifactId>
                <version>${jd3-econometrics.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-stats</artifactId>
                <version>${jd3-econometrics.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-arima</artifactId>
                <version>${jd3-econometrics.version}</version>            
            </dependency>
            <dependency>
               <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-benchmarking-datatypes</artifactId>
                <version>${jd3-benchmarking.version}</version>            
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>demetra-benchmarking</module>
        <module>demetra-benchmarking-datatypes</module>
    </modules>
    
    <!--build>
        <plugins>
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build-->
</project>