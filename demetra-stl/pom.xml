<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.europa.ec.joinup.sat</groupId>
        <artifactId>demetra-parent</artifactId>
        <version>3.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>demetra-stl</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>JD3 - STL</name>
    
    <modules>
        <module>demetra-stl-api</module>
        <module>demetra-stl-core</module>
        <module>demetra-stl-r</module>
        <module>demetra-stl-io</module>
    </modules>
  
    <properties>
        <demetra.version>3.0.0-SNAPSHOT</demetra.version>
        <jd3-toolkit.version>1.0.0-SNAPSHOT</jd3-toolkit.version>
        <jd3-sa.version>1.0.0-SNAPSHOT</jd3-sa.version>
        <jd3-stl.version>1.0.0-SNAPSHOT</jd3-stl.version>
        <jd3-highfreq.version>1.0.0-SNAPSHOT</jd3-highfreq.version>
    </properties>
    
    <dependencyManagement>  
        <dependencies>
            <!-- modules 3.0 > -->
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-toolkit-api</artifactId>
                <version>${jd3-toolkit.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-sa-api</artifactId>
                <version>${jd3-sa.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-highfreq-api</artifactId>
                <version>${jd3-highfreq.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-data-tests</artifactId>
                <version>${jd3-toolkit.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-toolkit-basic</artifactId>
                <version>${jd3-toolkit.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-stl-api</artifactId>
                <version>${jd3-stl.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-sa-core</artifactId>
                <version>${jd3-sa.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-highfreq-core</artifactId>
                <version>${jd3-highfreq.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-sa-information</artifactId>
                <version>${jd3-sa.version}</version>            
            </dependency>
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-stl-core</artifactId>
                <version>${jd3-stl.version}</version>            
            </dependency>
            <!-- < modules 3.0 -->
            <dependency>
                <groupId>eu.europa.ec.joinup.sat</groupId>
                <artifactId>demetra-tstoolkit</artifactId>
                <version>${jd2.version}</version>            
                <scope>test</scope>
            </dependency>
        </dependencies>   
    </dependencyManagement>
</project>